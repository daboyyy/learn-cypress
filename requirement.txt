npm i cypress -D
// add "cypress:open": "cypress open" into "script zone" inside package.json
npm i -D start-server-and-test
// add "test": "start-server-and-test dev 3000 cypress:open" into "script zone" inside package.json
